DATABASE_URL="postgres://postgres:mypassword@localhost:5432/agentlinkedin"
GEMINI_API_KEY=""
# Protects /api/cron/morning-run. Set any long random string; Vercel Cron
# sends it automatically as "Authorization: Bearer <CRON_SECRET>".
CRON_SECRET=""
# Signs login session cookies. Required for sign up / login to work at
# all — set a long random string, the same one in every environment that
# should share sessions (locally and on Vercel each get their own value).
AUTH_SECRET=""
